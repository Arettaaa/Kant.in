<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#CC000000"
    android:padding="24dp">

    <!-- Tombol tutup (X) pojok kanan atas -->
    <ImageView
        android:id="@+id/btnTutupQris"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:background="@drawable/bg_circle_outline"
        android:padding="8dp"
        android:src="@drawable/close"
        android:clickable="true"
        android:focusable="true" />

    <!-- Kartu putih tengah -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/bg_card_outline"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Badge QRIS + Judul -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:layout_marginBottom="8dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_badge_blue_light"
                android:text="QRIS"
                android:textColor="#2563EB"
                android:textSize="12sp"
                android:textStyle="bold"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:text="Scan untuk Bayar"
                android:textColor="#1A1A1A"
                android:textSize="15sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Nama kantin -->
        <TextView
            android:id="@+id/tvNamaKantinQris"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="16dp"
            android:text=""
            android:textColor="#6B7280"
            android:textSize="13sp" />

        <!-- Gambar QRIS ukuran besar -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="280dp"
            android:layout_marginBottom="20dp"
            app:cardCornerRadius="12dp"
            app:cardElevation="0dp">

            <ImageView
                android:id="@+id/ivQrisFull"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#F3F4F6"
                android:scaleType="fitCenter"
                android:padding="8dp" />
        </androidx.cardview.widget.CardView>

        <!-- Instruksi -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="20dp"
            android:gravity="center"
            android:text="Buka aplikasi bank atau e-wallet kamu\nlalu pindai kode QR di atas"
            android:textColor="#6B7280"
            android:textSize="13sp" />

        <!-- Tombol Download -->
        <TextView
            android:id="@+id/btnDownloadQrisFull"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@drawable/bg_btn_orange_solid"
            android:gravity="center"
            android:text="Simpan ke Galeri"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:textStyle="bold"
            android:clickable="true"
            android:focusable="true" />
    </LinearLayout>

</RelativeLayout>